import { PokemonHeldItemVersion } from './pokemon-held-item-version';

describe('PokemonHeldItemVersion', () => {
  it('should create an instance', () => {
    expect(new PokemonHeldItemVersion()).toBeTruthy();
  });
});
