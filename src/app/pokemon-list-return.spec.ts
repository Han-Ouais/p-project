import { PokemonListReturn } from './pokemon-list-return';

describe('PokemonListReturn', () => {
  it('should create an instance', () => {
    expect(new PokemonListReturn()).toBeTruthy();
  });
});
